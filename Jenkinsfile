pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'build the code'
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploy to app server'
      }
    }

  }
}